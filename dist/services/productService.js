"use strict";
// import { Product } from '../models/Product';
// export const createProduct = async (name: string, price: number, description: string, imageUrl: string) => {
//   const product = new Product({ name, price, description, imageUrl });
//   try {
//     await product.save();
//     return product;
//   } catch (error) {
//     throw new Error('Error creating product');
//   }
// };
// export const getProductById = async (id: string) => {
//   const product = await Product.findById(id);
//   if (!product) {
//     throw new Error('Product not found');
//   }
//   return product;
// };
// // Additional product-related methods (e.g., updateProduct, deleteProduct) can be added here
